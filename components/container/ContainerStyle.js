import { StyleSheet } from "react-native";

const contaier = StyleSheet.create({
  main: {
    backgroundColor: "#fff",
    width: "100%",
    height: "100%",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
});
export default contaier;
